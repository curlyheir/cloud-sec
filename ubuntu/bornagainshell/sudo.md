# sudo

- sudo stands for "Superuser Do". It allows regular users to execute commands as the superuser (or root user).

[ sudo usermod -aG groupname username ] adding user group

  sudo gpasswd -d <username> <groupname> delete user from group


